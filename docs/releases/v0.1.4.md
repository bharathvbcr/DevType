# DevType v0.1.4

DevType v0.1.4 makes the menu bar and secret search fast, contextual, and reliable during password entry: when macOS
Secure Input is active, it shows a key icon and **Copy Secret**. Clicking it opens **Search Secrets** directly,
authenticates via Touch ID (or login password), and copies the secret to the concealed clipboard for manual **⌘V** pasting.

## Password-field & Search Secrets experience

- **Direct Search Secrets**: Primary-clicking the menu bar button while Secure Input is active opens the **Search Secrets**
  palette directly with the search field focused. Right-clicking or Control-clicking opens the full status menu.
- **Single-click selection**: Choosing a secret in the search palette commits the clicked row with a single click.
- **Touch ID prompt reliability**: Fixed premature `LAContext` invalidation where closing the search palette caused DevType
  to resign active and abort the in-flight Touch ID evaluation. Each evaluation session now uses a dedicated `LAContext`.
- **Keyboard navigation**: Arrow keys navigate, Return and `⌘1`–`⌘9` jump shortcuts commit the selected secret.
- **Stability & status retention**: Focus changes, snippet edits, and preference changes cannot rearrange rows under the
  pointer while the menu is tracking; closing the menu restores current engine status.
- **Localized guidance**: Updated guidance in English, Korean, and Japanese, including VoiceOver accessibility labels.

## Reliability and security

- Every secret resolution flows through `SecretMenuFlow.resolve` — secrets are AES-GCM encrypted and never exposed in general storage, logs, or exports.
- `BiometricGate` drops its 30-second authorization reuse window whenever DevType goes to background (`lastSuccess = nil`), ensuring returning to an unlocked Mac requires re-authentication.
- In-flight evaluations requested by the user proceed without cancellation when the palette closes.
- Concealed clipboard markers (`org.nspasteboard.ConcealedType`, `com.agilebits.onepassword`) and 60-second auto-clear timer remain active.
- Typed triggers remain suppressed inside secure fields by design.

## Verification

- `./Scripts/test.sh`: **1,938 unit, stress, and contract tests passed**, **0 failures**, **14 opt-in skips** (live-AI and native window checks).
- Dedicated suites: **27/27 `BiometricGateTests`**, **84/84 Secret-related tests**, **6/6 native copy-palette tests**.
- AppKit presentation tests exercise 1,024 combinations, 10,000 focus transitions, and 2,000 click routings.
- `./Scripts/ci-local.sh`: All syntax, plist, self-tests, debug/release builds, packaging, and codesign verifications passed.

## Local build and distribution status

The local v0.1.4 build uses the existing **Apple Development** signing identity
and is **unnotarized**. The application version is stamped from the exact `v0.1.4` Git tag (`CFBundleShortVersionString: 0.1.4`).

The local installation target is `/Applications/DevType.app`.

